library;
/// Compatibility export for the auth animation layer.
/// The real palette logic lives in the shared theme file so the animation code does not duplicate it.
export '../../../core/theme/app_theme.dart'
    show AppColors, AppTheme, HoneycombThemeData;
